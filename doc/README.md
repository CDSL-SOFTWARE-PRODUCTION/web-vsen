# Tài liệu dự án (tối giản)

| File | Khi nào đọc |
| --- | --- |
| **[`guide.md`](guide.md)** | Hằng ngày: North Star, roadmap phase A–E, ma trận quyền + `C-*`, backlog, runbook migration, hành trình UX & edge cases. |
| **[`system_architecture.md`](system_architecture.md)** | Blueprint sâu + ERD (mermaid); đối chiếu với `model/*.yaml`. |

**SSOT kỹ thuật:** `model/` (YAML). Kiểm tra đồng bộ: `python3 scripts/audit_doc_model_consistency.py`.
